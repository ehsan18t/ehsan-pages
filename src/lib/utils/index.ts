// Utility functions barrel export
export { cn } from './cn';

/**
 * @deprecated gsapAnimations utilities are unused in the codebase.
 * Use `scrollReveal` action from `$lib/actions/scrollReveal` instead.
 */
export * from './gsapAnimations';
