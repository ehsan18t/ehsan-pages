const Skills = ({ itemRef }: any) => {
  return (
    <section
      className="flex flex-col items-center justify-between py-12 px-6 sm:px-12 md:px-32 lg:px-40 bg-gray-900 text-gray-200"
      ref={itemRef}
    >
      Skills
    </section>
  );
};

export default Skills;
