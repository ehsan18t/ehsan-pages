---
import { Icon } from "astro-icon/components";

interface Props {
  name: string;
  class?: string;
  showIcon?: boolean;
  [key: string]: any;
}

const { name, class: className, showIcon = true, ...rest } = Astro.props;

// Map of tech names to their icons
const techIcons: Record<string, string> = {
  html: "mdi:language-html5",
  css: "mdi:language-css3",
  javascript: "mdi:language-javascript",
  typescript: "mdi:language-typescript",
  react: "mdi:react",
  nextjs: "simple-icons:nextdotjs",
  svelte: "simple-icons:svelte",
  sveltekit: "simple-icons:svelte",
  redux: "simple-icons:redux",
  tailwindcss: "mdi:tailwind",
  astro: "simple-icons:astro",
  django: "simple-icons:django",
  php: "mdi:language-php",
  sql: "mdi:database",
  firebase: "mdi:firebase",
  python: "mdi:language-python",
  java: "mdi:language-java",
  cplusplus: "mdi:language-cpp",
  c: "mdi:language-c",
  git: "mdi:git",
  github: "mdi:github",
  linux: "mdi:linux",
  bash: "mdi:console",
  batch: "mdi:microsoft-windows",
  figma: "mdi:figma",
  vim: "simple-icons:vim",
  githubactions: "mdi:github-actions",
  jira: "mdi:jira",
  scrum: "mdi:chart-timeline",
  latex: "simple-icons:latex",
  markdown: "mdi:language-markdown-outline",
  google: "mdi:google",
  vercel: "simple-icons:vercel",
  stripeapi: "simple-icons:stripe",
  stripe: "simple-icons:stripe",
};

// Calculate slug for data attribute
const techSlug = name
  .toLowerCase()
  .replace(/\+/g, "plus")
  .replace(/[^a-z0-9]/g, "");

const iconName = techIcons[techSlug] || "mdi:code-tags";
---

<span class:list={["tech-pill", className]} data-tech={techSlug} {...rest}>
  {
    showIcon && (
      <span class="tech-icon">
        <Icon name={iconName} />
      </span>
    )
  }
  <span class="tech-name">{name}</span>
</span>

<style>
  /* Define CSS variables for commonly used values */
  :root {
    --pill-shadow-base: 0 1px 2px rgba(0, 0, 0, 0.04), 0 1px 1px rgba(0, 0, 0, 0.06);
    --pill-text-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
  }

  .tech-pill {
    display: inline-flex;
    align-items: center;
    font-size: 0.75rem;
    font-weight: 600;
    padding: 0.25rem 0.75rem;
    border-radius: 9999px;
    white-space: nowrap;
    position: relative;
    background: linear-gradient(135deg, oklch(var(--accent-500)), oklch(var(--accent-700)));
    color: white;
    box-shadow: var(--pill-shadow-base);
    overflow: hidden;
    text-shadow: var(--pill-text-shadow);
    user-select: none;
  }

  /* Tech icon styling */
  .tech-icon {
    display: inline-flex;
    margin-right: 0.375rem;
    filter: drop-shadow(0 1px 1px rgba(0, 0, 0, 0.15));
  }

  .tech-icon svg {
    width: 0.875rem;
    height: 0.875rem;
  }

  /* Frontend pill styles */
  .tech-pill[data-tech="html"] {
    background: linear-gradient(135deg, #e44d26, #c62f0e);
    color: white;
    box-shadow: 0 2px 5px rgba(228, 77, 38, 0.3);
  }

  /* Keep all the remaining tech-specific styles as they were... */
  .tech-pill[data-tech="css"] {
    background: linear-gradient(135deg, #1572b6, #0a4876);
    color: white;
    box-shadow: 0 2px 5px rgba(21, 114, 182, 0.3);
  }

  .tech-pill[data-tech="javascript"] {
    background: linear-gradient(135deg, #f7df1e, #e6c700);
    color: #000;
    box-shadow: 0 2px 5px rgba(247, 223, 30, 0.3);
    text-shadow: none;
    font-weight: 700;
  }

  .tech-pill[data-tech="typescript"] {
    background: linear-gradient(135deg, #3178c6, #1a4373);
    color: white;
    box-shadow: 0 2px 5px rgba(49, 120, 198, 0.3);
  }

  .tech-pill[data-tech="react"] {
    background: linear-gradient(135deg, #61dafb, #06a5cb);
    color: #000;
    box-shadow: 0 2px 5px rgba(97, 218, 251, 0.3);
    text-shadow: none; /* Remove text shadow on light backgrounds */
    font-weight: 700; /* Bold for better visibility on light blue */
  }

  .tech-pill[data-tech="nextjs"] {
    background: linear-gradient(135deg, #191818, #070707); /* Darker gradient */
    color: white;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
  }

  .tech-pill[data-tech="svelte"] {
    background: linear-gradient(135deg, #ff3e00, #d73500); /* Slightly darker end */
    color: white;
    box-shadow: 0 2px 5px rgba(255, 62, 0, 0.3);
  }

  .tech-pill[data-tech="sveltekit"] {
    background: linear-gradient(135deg, #ff3e00, #c73400); /* Darker end for contrast */
    color: white;
    box-shadow: 0 2px 5px rgba(255, 62, 0, 0.3);
  }

  .tech-pill[data-tech="redux"] {
    background: linear-gradient(135deg, #764abc, #4c2b9e); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(118, 74, 188, 0.3);
  }

  .tech-pill[data-tech="tailwindcss"] {
    background: linear-gradient(135deg, #06b6d4, #0369a1); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(6, 182, 212, 0.3);
  }

  .tech-pill[data-tech="astro"] {
    background: linear-gradient(135deg, #ff5d01, #d54500); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(255, 93, 1, 0.3);
  }

  /* Backend & Data - Enhanced contrast */
  .tech-pill[data-tech="django"] {
    background: linear-gradient(135deg, #0c4b33, #033323); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(12, 75, 51, 0.3);
  }

  .tech-pill[data-tech="php"] {
    background: linear-gradient(135deg, #777bb4, #5a5d99); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(119, 123, 180, 0.3);
  }

  .tech-pill[data-tech="sql"] {
    background: linear-gradient(135deg, #336791, #1a4c7a); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(51, 103, 145, 0.3);
  }

  .tech-pill[data-tech="firebase"] {
    background: linear-gradient(135deg, #ffca28, #f08700); /* Darker end color */
    color: #000; /* Dark text for better contrast */
    box-shadow: 0 2px 5px rgba(255, 202, 40, 0.3);
    text-shadow: none; /* Remove text shadow on light backgrounds */
    font-weight: 700;
  }

  .tech-pill[data-tech="python"] {
    background: linear-gradient(135deg, #3776ab, #1a4971); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(55, 118, 171, 0.3);
  }

  /* Cloud & Deployment - Better visibility */
  .tech-pill[data-tech="vercel"] {
    background: linear-gradient(135deg, #222222, #000000);
    color: white;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
  }

  .tech-pill[data-tech="stripeapi"] {
    background: linear-gradient(135deg, #6772e5, #4257d9); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(103, 114, 229, 0.3);
  }

  .tech-pill[data-tech="fastapi"] {
    background: linear-gradient(135deg, #009688, #00695c); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(0, 150, 136, 0.3);
  }

  /* VCS & Tools - Improved contrast */
  .tech-pill[data-tech="git"] {
    background: linear-gradient(135deg, #f34f29, #c03716); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(243, 79, 41, 0.3);
  }

  .tech-pill[data-tech="github"] {
    background: linear-gradient(135deg, #24292e, #121417); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(36, 41, 46, 0.3);
  }

  .tech-pill[data-tech="githubactions"] {
    background: linear-gradient(135deg, #2088ff, #0366d6); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(32, 136, 255, 0.3);
  }

  .tech-pill[data-tech="linux"] {
    background: linear-gradient(135deg, #fcc624, #e6ac00); /* Darker end color */
    color: #000; /* Dark text for better contrast */
    box-shadow: 0 2px 5px rgba(252, 198, 36, 0.3);
    text-shadow: none;
    font-weight: 700;
  }

  .tech-pill[data-tech="bash"] {
    background: linear-gradient(135deg, #3c3c3c, #1e1e1e); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(60, 60, 60, 0.3);
  }

  .tech-pill[data-tech="batch"] {
    background: linear-gradient(135deg, #0078d6, #005fb0); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(0, 120, 214, 0.3);
  }

  /* Design - Better contrast */
  .tech-pill[data-tech="figma"] {
    background: linear-gradient(135deg, #f24e1e, #c83400); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(242, 78, 30, 0.3);
  }

  /* Editors - Improved visibility */
  .tech-pill[data-tech="vim"] {
    background: linear-gradient(135deg, #019833, #006622); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(1, 152, 51, 0.3);
  }

  /* Project Management - Better readability */
  .tech-pill[data-tech="jira"] {
    background: linear-gradient(135deg, #0052cc, #00399d); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(0, 82, 204, 0.3);
  }

  .tech-pill[data-tech="scrum"] {
    background: linear-gradient(135deg, #6554c0, #473990); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(101, 84, 192, 0.3);
  }

  /* Documentation - Enhanced contrast */
  .tech-pill[data-tech="latex"] {
    background: linear-gradient(135deg, #008080, #005959); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(0, 128, 128, 0.3);
  }

  .tech-pill[data-tech="markdown"] {
    background: linear-gradient(135deg, #083fa1, #042a6d); /* Darker end color */
    color: white;
    box-shadow: 0 2px 5px rgba(8, 63, 161, 0.3);
  }

  /* Special gradients with improved visibility */
  .tech-pill[data-tech="google"] {
    background: linear-gradient(135deg, #4285f4, #34a853, #fbbc05, #ea4335);
    color: white; /* Keep white for this special case */
    box-shadow: 0 2px 5px rgba(66, 133, 244, 0.3);
    background-size: 300% 100%;
    animation: google-gradient 3s ease infinite;
    font-weight: 700; /* Bolder for better legibility since it's moving */
    /* Add a subtle black glow behind the text for better visibility as the colors shift */
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.3);
  }

  /* Add animation for filterable pills */
  @media (prefers-reduced-motion: no-preference) {
    .tech-pill[role="button"] {
      transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
    }

    .tech-pill[role="button"]:hover {
      animation: pulse 1s infinite;
    }

    @keyframes pulse {
      0% {
        box-shadow: 0 0 0 0 rgba(var(--accent-500), 0.7);
      }
      70% {
        box-shadow: 0 0 0 6px rgba(var(--accent-500), 0);
      }
      100% {
        box-shadow: 0 0 0 0 rgba(var(--accent-500), 0);
      }
    }
  }

  /* Respect reduced motion preferences */
  @media (prefers-reduced-motion: reduce) {
    .tech-pill {
      transition: none;
    }
  }
</style>
