/** @type {import('next').NextConfig} */
const nextConfig = {
  head: {
    meta: [
      {
        name: 'color-scheme',
        content: 'dark',
      },
    ],
  },
};

module.exports = nextConfig;
